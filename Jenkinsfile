pipeline {
    agent any
    
    stages {
        
        stage('github-clone') {
            steps {
                git branch: 'main', credentialsId: 'github-test', url: 'https://github.com/gnzmzn321/jenkins-test.git'
            }
        }
        
   		// stage...
   	}
}
